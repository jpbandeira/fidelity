server:
  host: 0.0.0.0
  port: 8080

platform: "local-env"
database_type: "postgres"

postgres:
  host: "postgres"
  port: "5432"
  database: "fidelity"
  username: "fidelity"
  password: "suaSenhaSeguraAqui"
  sslmode: "disable"

log:
  level: "debug"
